name: Deploy branch on push

on:
  push: 
    branches: 
      -main

jobs:

  job1: 
    steps: 
      - name: checkout repo 
        uses: actions/checkout@v4
      
      - id: step1
        run: echo "test=hello" >> "$GITHUB_OUTPUT"
