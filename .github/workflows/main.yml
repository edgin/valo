name: Deploy branch on push

on:
  push: 
    branches: 
      -main

jobs: 
  deploy:  
    steps: 
      - name: checkout repo 
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
