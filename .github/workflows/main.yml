name: Deploy branch on push

on:
  push: 
    branches: 
      -main

jobs:
  test:
    steps:
      - name: List files in the repository
        run: |
          ls ${{ github.workspace }}
      - run: echo "🍏 This job's status is ${{ job.status }}."