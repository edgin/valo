name: Deploy branch on push

on:
  push: 
    branches: 
      -main

jobs:
  steps: 
    - name: checkout repo 
      uses: actions/checkout@v4
    - name: Every time
      run: echo "This step will always run"
